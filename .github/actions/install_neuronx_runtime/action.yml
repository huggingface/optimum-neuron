name: Install Neuronx SDK
description: install system and python packages for an AWS Neuronx SDK version
runs:
    using: "composite"
    steps:
      - name: Install Neuronx system packages
        shell: bash
        run: |
          . /etc/os-release
          sudo tee /etc/apt/sources.list.d/neuron.list > /dev/null <<EOF
          deb https://apt.repos.neuron.amazonaws.com ${VERSION_CODENAME} main
          EOF
          wget -qO - https://apt.repos.neuron.amazonaws.com/GPG-PUB-KEY-AMAZON-AWS-NEURON.PUB | sudo apt-key add -
          sudo apt-get update -y
          sudo apt-get install aws-neuronx-tools=2.28.23.0-f1c114a9d aws-neuronx-runtime-lib=2.30.51.0-faafe26f0 aws-neuronx-collectives=2.30.59.0-f5cdefb39 -y
          export PATH=/opt/aws/neuron/bin:$PATH
          dpkg -l | grep neuron
      - name: Display driver version
        shell: bash
        run: |
          cat /sys/module/neuron/version || echo "Neuron driver version not available"
